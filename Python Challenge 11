s=raw_input("Enter sentences with lower and upper case letters: ")
d={"UPPER":0, "LOWER":0}
for c in s:
	if c.isupper():
		d["UPPER"]+=1
	elif c.islower():
		d["LOWER"]+=1
	else:
		pass

print "Upper", d["UPPER"]
print "Lower", d["LOWER"]
